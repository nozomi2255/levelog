# Levelog user-first implementation and handoff plan

Last updated: 2026-07-20 20:10 (Asia/Tokyo)

This document is the source of truth for implementation and session handoff. Only the orchestrator updates task status and integration notes. Agents report results to the orchestrator and do not edit this file.

## Product objective

Levelog is a local macOS growth OS that turns daily work and learning into user-confirmed growth evidence and a safe next challenge. The product must not treat AI output as fact or convert activity volume directly into skill mastery.

The original vertical-slice baseline was complete when a user could:

1. Configure a role, one to three focus skills, weekly time budget, excluded quest patterns, and a Codex CLI path.
2. Save an activity using any of: what they did, what was difficult, or what changed.
3. Review and optionally redact the exact payload before it is sent to Codex.
4. Review, edit, accept, or reject AI analysis candidates.
5. Confirm analysis so accepted evidence and deterministic account XP are recorded atomically.
6. Receive, accept, adjust, postpone, cancel, complete, and reflect on a safe quest.
7. See real activity, XP, quest, and evidence data in the mock-inspired dashboard.
8. Create a safe SQLite backup and a versioned JSON export.

## Historical baseline limits

These limits describe the completed baseline, not a product-quality ceiling. Phase 3 may replace a
baseline shortcut whenever the user-first growth loop or `docs/growth-log-system-design.md` calls
for a stronger design. Unlisted expansion is still recorded as a separate task before implementation.

- App Store distribution, signing, or notarization
- Windows or mobile support
- External integrations such as Obsidian, GitHub, Calendar, Slack, or cloud sync
- Social, ranking, guild, or organization features
- Weekly review, calibration quests, automatic skill rank promotion, semantic search, or a vector database
- Fully local inference; Codex with ChatGPT login may use cloud inference
- Functional search and notification systems in the first release

## Technology and identity

- App: Levelog
- Bundle identifier: `com.levelog.desktop`
- Desktop: Tauri 2 on Apple Silicon macOS
- Frontend: React 19, TypeScript, Vite, Tailwind CSS 4, React Router, TanStack Query, React Hook Form, Zod, Lucide
- Backend: Rust, Tokio, serde, sqlx/SQLite, schemars, thiserror, tracing
- LLM boundary: Codex CLI `codex exec`
- Tests: Vitest, Testing Library, Playwright, Rust unit/integration tests
- Package manager: pnpm

The repository uses these top-level areas:

```text
src/                         React application
src-tauri/src/               Rust commands, application, domain, infrastructure
src-tauri/migrations/        append-only SQLite migrations
src-tauri/schemas/           versioned Codex output schemas
src-tauri/prompts/           versioned prompts
tests/                       browser and shared fixtures
docs/                        requirements, design, mock, this handoff plan
```

## Design system

- Base background: deep navy, never pure black.
- Primary system color: cyan blue; success/growth: green; primary action/reward: amber-gold; special proposals: purple.
- Panels use thin visible borders and clipped corners. Blur is limited; hierarchy comes from borders, gradients, and restrained inner glow.
- Typography uses the macOS system Japanese stack and a system monospace stack for numbers. No runtime font download.
- All controls use semantic elements, visible focus, minimum 44x44 hit areas, labels, and non-color state indicators.
- Motion uses only transform/opacity, 150-300 ms, and respects `prefers-reduced-motion`.
- Charts and the growth core use CSS/SVG. No R3F or chart dependency.
- Responsive layout:
  - `>=1360px`: three-column mock-style dashboard
  - `1100-1359px`: two-column dashboard
  - `1024-1099px`: icon sidebar and vertically stacked panels
- Initial Tauri window: 1440x900; minimum: 1024x720.

Implemented routes:

```text
/onboarding
/
/activities
/activities/new
/activities/:activityId/analysis
/quests
/skills
/settings
/reports   (explicit next-phase empty state)
/memory
/memory/import
/memory/claims
/projects
/projects/:projectId
/sources/:sourceId
/portfolio
```

## Fixed domain rules

### Skills

The internal skill catalog is fixed to 15 IDs across five categories:

```text
thinking.information_structuring
thinking.problem_decomposition
thinking.hypothesis_testing
technical.technical_learning
technical.system_design
technical.validation
communication.clarification
communication.explanation
communication.documentation
execution.prioritization
execution.planning
execution.follow_through
interpersonal.listening
interpersonal.alignment
interpersonal.feedback
```

AI can only reference these IDs. The first MVP displays evidence count and `observing`, not a synthetic mastery rank.

### XP and levels

- Activity saved: `+10`, once per activity.
- Analysis confirmed: `+20`, once per analysis/activity confirmation.
- Quest reflection saved: `+40`, once per quest, regardless of outcome.
- Candidate count, accepted evidence count, and quest success do not grant XP.
- Cumulative XP required to reach level L: `50 * (L - 1) * L`.
- XP is an append-only ledger and every award has a unique reason key.

### Quest state

Allowed states:

```text
proposed -> accepted -> in_progress -> completed
proposed|accepted|in_progress -> rescheduled
proposed|accepted|in_progress -> adjusted
proposed|accepted|in_progress -> cancelled
rescheduled|adjusted -> accepted|in_progress|cancelled
```

Rescheduling and adjustment are not failures. A reflection can record `completed`, `partially_completed`, `not_completed`, or `rested`; each is equally eligible for the reflection XP event.

Quest generation uses a versioned, safe template catalog. A generated quest must be within the user's authority, short, observable, reversible, and avoid unnecessary burden on other people.

## Persistence contract

The initial migration creates:

- `activities`
- `skills`
- `ai_analyses`
- `skill_candidates`
- `skill_observations`
- `quests`
- `quest_reflections`
- `xp_events`
- `app_settings`

Invariants:

- IDs are UUID strings; timestamps are UTC ISO 8601; activity occurrence also stores a local calendar date.
- `PRAGMA foreign_keys = ON`, WAL, and 5000 ms busy timeout are enabled on every pool.
- User-entered activity fields are immutable after AI analysis begins. Corrections create an explicit new revision or new activity; AI never overwrites raw text.
- Submitted/redacted payload, raw AI result, provider, model, Codex version, prompt version, and schema version are retained with the analysis.
- AI candidates remain separate from user decisions and confirmed observations.
- Analysis confirmation and its XP/evidence writes are one transaction and idempotent.
- Migrations are append-only after first release.

Backup and export:

- Database: Tauri's Application Support app-data directory (`com.levelog.desktop/levelog.db`).
- On startup, at most once per local day, create a consistent backup with SQLite `VACUUM INTO`.
- Manual backup uses the same mechanism.
- JSON export includes schema version, export timestamp, settings excluding future secrets, raw activities, analysis/user decisions, observations, quests/reflections, and XP events.
- Restore/import UI is not part of the first MVP.

## Tauri command and DTO contract

The frontend can invoke only purpose-specific commands; it never receives raw SQL or arbitrary process execution.

```text
get_boot_state() -> BootState
save_onboarding(OnboardingInput) -> AppSettingsDto
update_codex_path(CodexConnectionInput) -> AppSettingsDto
test_codex_connection(CodexConnectionInput) -> CodexConnectionStatus

get_dashboard() -> DashboardSnapshot
list_skills() -> SkillDto[]
create_activity(CreateActivityInput) -> ActivityDto
list_activities() -> ActivityDto[]
get_activity(activityId) -> ActivityDetailDto

get_analysis_preview(activityId) -> AnalysisPreview
start_activity_analysis(StartAnalysisInput) -> AnalysisJobDto
get_activity_analysis(analysisId) -> ActivityAnalysisDto
cancel_activity_analysis(analysisId) -> AnalysisJobDto
confirm_activity_analysis(ConfirmAnalysisInput) -> ConfirmAnalysisResult

generate_quest(GenerateQuestInput) -> QuestDto
list_quests() -> QuestDto[]
transition_quest(QuestTransitionInput) -> QuestDto
save_quest_reflection(QuestReflectionInput) -> QuestReflectionDto

create_backup() -> BackupResult
export_json() -> ExportResult
```

`AnalysisPreview` contains the exact JSON to be submitted and an editable `submittedPayload`. Editing it does not edit the source activity.

`ActivityAnalysis` schema v1 contains:

```text
summary: string
outcomes: string[]
skillCandidates: [{ skillId, confidence 0..1, reason, evidence }]
missingInformationQuestion: string|null
```

`QuestProposal` schema v1 contains:

```text
templateId: string
title: string
description: string
targetSkillId: string
estimatedMinutes: integer
difficulty: integer 1..5
successCriteria: string[]
evidencePrompt: string
```

## Codex execution boundary

- Save the activity before starting inference.
- Resolve and persist an absolute Codex path; verify `codex --version` and `codex login status`.
- Execute directly with `tokio::process::Command`, never through a shell.
- Use a new empty temporary working directory for every call.
- Required controls: ephemeral run, read-only sandbox, ignore user config, ignore rules, skip Git check, disable Web search, disable shell/unified exec/browser/computer tools, output schema, and output-last-message.
- If the installed CLI does not support a required safety control, return a compatibility error. Never silently remove the control.
- Only one inference job runs at a time. Timeout is 180 seconds. It is cancellable.
- Retry once only when output is missing or violates the schema.
- On startup, stale `running` jobs become `failed` with an interrupted reason and can be retried.
- Runtime has no mock inference provider. Tests inject a fake `ProcessRunner`.

## Task ledger

Allowed status values: `pending`, `in_progress`, `blocked`, `completed`.

| ID | Owner | Status | Depends on | Area |
| --- | --- | --- | --- | --- |
| P0-DOC | Orchestrator | completed | none | This plan and handoff contract |
| P0-SCAFFOLD | Orchestrator | completed | P0-DOC | Git, Tauri/React/Rust scaffold, shared configs |
| P0-CONTRACTS | Orchestrator | completed | P0-SCAFFOLD | shared DTO/types, tokens, command registration skeleton |
| W1-A | Agent A | completed | P0-CONTRACTS | app shell, router, navigation, HUD primitives, SVG charts |
| W1-B | Agent B | completed | P0-CONTRACTS | migrations, DB, repositories, XP and quest domain rules |
| W1-C | Agent C | completed | P0-CONTRACTS | Codex path/probe, runner, schemas, prompts, job lifecycle |
| INT-1 | Orchestrator | completed | W1-A/B/C | Wave 1 integration and build |
| W2-D | Agent D | completed | INT-1 | onboarding, activity, preview, analysis confirmation UI |
| W2-E | Agent E | completed | INT-1 | dashboard, skills, quests and reflection UI |
| W2-F | Agent F | completed | INT-1 | settings, backup/export, accessibility and test coverage |
| INT-2 | Orchestrator | completed | W2-D/E/F | full integration and behavior fixes |
| VERIFY | Orchestrator | completed | INT-2 | lint, tests, build, Tauri bundle, document closure |

## Phase 3: user-first growth loop

Phase 3 is the active product direction. It is not constrained to the smallest MVP surface. The
experience principles in `docs/growth-log-system-design.md` take precedence when they conflict with
the original three-field capture flow, while the existing human-confirmation, append-only evidence,
XP idempotency, local-data, and fail-closed Codex security invariants remain mandatory.

The completed user outcome for this phase is:

```text
editable personal context
  -> one-line immutable capture
  -> exact cloud payload preview
  -> at most one high-value AI question
  -> answer / unknown / skip / defer
  -> editable growth-log candidate
  -> user-confirmed canonical evidence
  -> personalized but safe next quest
```

### Phase 3 architecture decisions

- Codex is discovered from a bounded macOS candidate set without a shell or login-shell PATH.
  Detection canonicalizes existing executable files; probing and saving remain explicit user actions.
- Capture must remain usable with no Codex installation. AI actions fail with actionable Japanese
  guidance, but the immutable capture and its account XP remain stored.
- Growth profile and Codex connection settings are separate responsibilities. Profile revisions are
  append-only and optimistic-concurrency protected. Existing `app_settings.profile` JSON is lazily
  migrated without deletion.
- Explicit user context includes role, responsibilities, domains/technologies, growth goals,
  recurring challenges, recent success, time budget, preferred quest duration/style, constraints,
  fixed focus skills, and free-form focus themes. AI assumptions never become profile facts.
- The fixed five-category / fifteen-skill catalog remains the stable canonical ontology. A candidate
  may also carry a user-facing specialized skill name, but every specialized name must map to one
  valid canonical skill and cannot create XP or mastery by itself.
- Activity workflow state is separate from Codex job state. The persisted minimum workflow is
  `captured | analysis_running | needs_input | assessable | review_pending | confirmed | excluded`.
- The activity-analysis v2 output separates confirmed facts, unconfirmed facts, outcomes, skill
  candidates, and one optional structured next question. The question includes a target, answer type,
  choices, and why the answer matters. The UI always adds unknown, skip, and defer controls.
- Interview answers are append-only. Re-analysis creates a new analysis/structure revision and never
  overwrites the capture, previous payload, previous raw output, or previous answer.
- Quest generation receives only confirmed evidence plus the current explicit profile/focus themes.
  Its exact payload is previewed before submission just like activity analysis.
- JSON export moves to schema v2 and explicitly includes raw captures, submitted payloads, AI raw
  output, profile revisions, focus themes, workflows, structures, and interview answers. The UI warns
  that these records may contain confidential user text even though authentication secrets are omitted.

### Phase 3 additive persistence

Append-only migration `0002_user_growth_context.sql` adds:

```text
user_profile_revisions
focus_themes
focus_theme_skill_links
activity_captures
activity_workflows
activity_structures
interview_sessions
interview_answers
quest_generation_runs
```

It also adds nullable specialized-skill fields to candidate/observation records and an optional focus
theme reference to quests. Migration must preserve every existing row and keep migration `0001`
unchanged.

### Phase 3 command and DTO additions

```text
get_user_profile() -> UserProfileDto
update_user_profile(UpdateUserProfileInput) -> UserProfileDto
discover_codex_candidates() -> CodexPathCandidateDto[]

quick_capture_activity(QuickCaptureInput) -> ActivityDto
list_activity_inbox() -> ActivityDto[]
get_activity_workflow(activityId) -> ActivityWorkflowDto
answer_activity_question(InterviewAnswerInput) -> ActivityWorkflowDto

get_quest_preview(GenerateQuestInput) -> AnalysisPreview
generate_quest(GenerateQuestInput{submittedPayload}) -> QuestDto

list_focus_themes() -> FocusThemeDto[]
save_focus_themes(SaveFocusThemesInput) -> FocusThemeDto[]
```

The legacy `save_onboarding`, `create_activity`, `test_codex_connection`, and stored profile format
remain readable compatibility entry points until all existing installations have migrated.

### Phase 3 task ledger

| ID | Owner | Status | Depends on | Exclusive change area |
| --- | --- | --- | --- | --- |
| P3-DOC | Orchestrator | completed | VERIFY | this Phase 3 plan and contracts |
| P3-CONTRACTS | Orchestrator | completed | P3-DOC | shared migration, Rust DTOs, TS types/API, registrations |
| P3-A | Agent A | completed | P3-CONTRACTS | Codex discovery and versioned profile backend |
| P3-B | Agent B | completed | P3-CONTRACTS | capture/workflow/interview/specialized-skill backend |
| P3-C | Agent C | completed | P3-CONTRACTS | profile, Codex setup, skills UI and shared HUD accessibility |
| P3-INT-1 | Orchestrator | completed | P3-A/B/C | schema/Codex adapter integration and Wave 1 validation |
| P3-D | Agent D | completed | P3-INT-1 | quick capture, inbox, structured question and review UI |
| P3-E | Agent E | completed | P3-INT-1 | export v2, quest payload preview, privacy copy and DB tests |
| P3-F | Agent F | completed | P3-INT-1 | Testing Library, Playwright, keyboard and responsive quality |
| P3-INT-2 | Orchestrator | completed | P3-D/E/F | complete frontend/backend integration and regression repair |
| P3-REVIEW | Independent reviewer | completed | P3-INT-2 | security, privacy, a11y and domain-invariant review |
| P3-VERIFY | Orchestrator | completed | P3-REVIEW | all verification gates, bundle and handoff closure |

Phase 3 acceptance adds these gates to the existing verification list:

- Existing profile JSON migrates to revision 1 without losing Codex configuration.
- A missing, directory, relative, or non-executable Codex path cannot be saved as ready.
- One-line capture works offline and remains immutable across cancellation, failure, and re-analysis.
- At most one unanswered interview question exists for an activity; answer, unknown, skip, and defer
  are keyboard operable and idempotent.
- No observation or analysis-confirmed XP is written before explicit final confirmation.
- Specialized skill names cannot bypass the canonical fifteen-ID allowlist.
- Activity and quest payload previews show the exact JSON that will be sent.
- Export v2 discloses its sensitive-content scope and includes all Phase 3 tables without future secrets.
- Settings, capture, inbox, interview, review, skills, and quest-preview flows reflow at all three desktop
  viewport baselines and remain usable with reduced motion.

Agent ownership is exclusive during a wave. Agents must not edit package manifests, shared DTO contracts, migration files outside their assignment, or this plan without orchestrator approval.

## Phase 4: personal evidence graph

Phase 4 implements the source/provenance model in `docs/personal-evidence-graph-design.md`. It is a
user-value phase rather than a reduced MVP: the completed vertical experience preserves imported
originals, separates facts from inferences, makes every consequential link reversible, and can compose
a private portfolio draft without turning raw notes or AI suggestions into public claims.

The fixed five-category/fifteen-skill catalog remains a stable observation lens for continuity and
quest targeting. It is not the ontology of the personal graph. Sources, projects, claims, outcomes,
decisions, technologies, specialized skills, and relationships are open-ended. Imported knowledge or
activity volume never awards XP and never becomes a skill observation without an explicit user decision.

The Phase 4 journey is:

```text
paste or select local Markdown/text
  -> save immutable local original and occurrence provenance
  -> inspect source and create/review excerpt-backed claim
  -> accept, edit, reject, exclude, or defer
  -> explicitly link accepted claim to a project
  -> compose an editable, private portfolio draft from accepted, sourced claims
```

### Phase 4 architecture and privacy decisions

- `/memory` becomes the Evidence Library. `/memory/import`, `/memory/claims`, `/projects`,
  `/sources/:sourceId`, and `/portfolio` are functional routes; Reports remains the next-phase surface.
- The first safe import handlers accept pasted UTF-8 and user-selected individual `.md`, `.markdown`,
  and `.txt` files. Folder recursion, ZIP, PDF, Word, CSV/JSON interpretation, Obsidian link-graph
  parsing, drag-and-drop paths, and background filesystem watching remain disabled until format-specific
  lossless adapters and resource limits are implemented.
- File paths come only from the Rust-native picker. The webview cannot submit an arbitrary filesystem
  path. Imports reject symlinks, non-regular files, NUL bytes, invalid UTF-8, unsupported extensions,
  files over 1 MiB, more than 100 files, or a batch over 10 MiB.
- A canonical source is content-addressed with SHA-256. Re-importing identical bytes creates a new
  occurrence record for provenance but never mutates or duplicates the original content.
- Raw source text is local and immutable. Claim edits create a successor revision linked by
  `supersedes_claim_id`; they do not rewrite a machine or user-authored candidate.
- Claim kind and provenance are independent of review state. Accepting an `ai_inference` does not
  relabel it as a fact. Knowledge notes are never direct skill-performance evidence.
- A claim can enter a project only through an explicit link action and can be unlinked. Similar project
  names are never silently merged.
- Portfolio drafts are private, editable local documents. Initial generated items require accepted,
  non-excluded claims with live provenance. Raw originals, pending/rejected claims, inference-only claims,
  and knowledge notes are excluded from automatic draft composition.
- JSON export becomes private archive schema v3 and includes Phase 4 raw sources and provenance. The
  settings UI must keep the explicit sensitive-content warning. A future share/public export is a
  separate redacted product and must exclude raw sources by default.
- Optional Codex extraction uses a separately versioned prompt/schema, exact payload preview, the same
  fail-closed process runner, a single global permit, 180-second timeout, cancellation, and one
  schema-only retry. Source content is untrusted data; Codex receives no shell, browser, computer, or
  Web-search capability. No extraction result is committed as fact, profile, project, portfolio, XP,
  or skill observation before review.

### Phase 4 additive persistence

Append-only migration `0003_personal_evidence_graph.sql` adds:

```text
source_documents                 immutable canonical UTF-8 originals and content hash
source_occurrences               every paste/file ingestion event and display-only origin
evidence_claims                  excerpt-backed facts, experiences, achievements, notes, and inferences
evidence_relations               typed, reversible claim-to-claim relations
evidence_claim_activity_links    explicit claim/activity provenance links
evidence_claim_skill_links       optional canonical skill mapping; never an observation or XP event
projects                         user-owned open-ended project records
project_evidence_links           explicit reversible accepted-claim links
portfolio_drafts                 private editable purpose-specific drafts
portfolio_draft_items            ordered claim provenance used by each draft
evidence_analysis_jobs           exact payload, raw output, status, prompt/schema versions, and errors
```

Foreign keys use `RESTRICT` for immutable source provenance. There is no source-delete command in this
phase; `excluded_from_assessment` is reversible and preserves the original. All tables use UUID text IDs
and UTC timestamps. Migrations remain append-only.

### Phase 4 command and DTO contract

```text
import_pasted_source(ImportPastedSourceInput) -> SourceImportResult
pick_and_import_sources() -> SourceImportResult
list_evidence_library(EvidenceLibraryQuery) -> EvidenceLibraryDto
get_evidence_source(sourceId) -> SourceDocumentDetailDto

create_evidence_claim(CreateEvidenceClaimInput) -> EvidenceClaimDto
review_evidence_claim(ReviewEvidenceClaimInput) -> EvidenceClaimDto
link_claim_to_activity(ClaimActivityLinkInput) -> EvidenceClaimDto
list_evidence_relations() -> EvidenceRelationDto[]
create_evidence_relation(CreateEvidenceRelationInput) -> EvidenceRelationDto
delete_evidence_relation(relationId) -> ()

create_project(CreateProjectInput) -> ProjectDto
list_projects() -> ProjectDto[]
get_project(projectId) -> ProjectDetailDto
link_claim_to_project(ProjectClaimLinkInput) -> ProjectDetailDto
unlink_claim_from_project(ProjectClaimLinkInput) -> ProjectDetailDto

create_portfolio_draft(CreatePortfolioDraftInput) -> PortfolioDraftDto
update_portfolio_draft(UpdatePortfolioDraftInput) -> PortfolioDraftDto
list_portfolio_drafts() -> PortfolioDraftDto[]

get_evidence_analysis_preview(sourceId) -> SubmissionPreview
start_evidence_analysis(StartEvidenceAnalysisInput) -> EvidenceAnalysisJobDto
get_evidence_analysis(jobId) -> EvidenceAnalysisJobDto
cancel_evidence_analysis(jobId) -> EvidenceAnalysisJobDto
```

Allowed claim kinds are `fact | experience | achievement | outcome | decision | lesson | knowledge |
idea | project | interest | personality_signal | inference`. Allowed provenance values are
`user_asserted | import_extracted | ai_inference | activity_confirmed`. Review states are
`pending | accepted | edited | rejected | excluded | deferred`. Only `accepted` and `edited` claims
may be project/portfolio inputs, and portfolio auto-composition additionally excludes `knowledge`,
`idea`, `personality_signal`, and `inference`.

Evidence extraction schema `evidence-extraction.v1` emits candidate statement, kind, provenance,
confidence, exact excerpt, optional UTF-8 byte anchors, and optional canonical skill/project hints.
Extraction candidates are always `pending`; project hints do not create projects or links.

### Phase 4 task ledger

| ID | Owner | Status | Depends on | Exclusive change area |
| --- | --- | --- | --- | --- |
| P4-DISCOVERY | 3 exploration agents | completed | P3-VERIFY | data model, import security, UX/IA read-only review |
| P4-DOC | Orchestrator | completed | P4-DISCOVERY | this Phase 4 plan and handoff contract |
| P4-CONTRACTS | Orchestrator | completed | P4-DOC | migration, shared Rust/TypeScript DTO/API contracts, registrations |
| P4-A | Data graph agent | completed | P4-CONTRACTS | evidence persistence, claims, projects, portfolios, archive v3 |
| P4-B | Secure ingestion agent | completed | P4-CONTRACTS | native file intake, extraction boundary, schema/prompt, job lifecycle |
| P4-C | Evidence UX agent | completed | P4-CONTRACTS | evidence/import/claim/project/source/portfolio React routes and tests |
| P4-INT | Orchestrator | completed | P4-A/B/C | command registration, type integration, regression repair |
| P4-REVIEW | Independent reviewer | completed | P4-INT | requirements, truthfulness, security, privacy, a11y review |
| P4-REWORK | Assigned from findings | completed | P4-REVIEW | bounded remediation if acceptance is not met |
| P4-VERIFY | Orchestrator | completed | P4-REWORK | all gates, bundle, handoff closure |

Phase 4 acceptance requires:

- Identical imports are content-idempotent while every occurrence remains auditable.
- Imported source bytes are unchanged after claim edit, rejection, exclusion, project unlink, or draft edit.
- Every claim exposes kind, provenance, review state, and source excerpt. Inferences never display as facts.
- No import/review/project/portfolio operation creates XP or `skill_observations`.
- File intake cannot read an arbitrary webview-provided path and enforces type/size/symlink/UTF-8 limits.
- Codex failure, timeout, cancellation, invalid JSON, or schema error keeps the local original and provides retry.
- Project links are explicit and reversible. Portfolio composition rejects unconfirmed, excluded,
  inference-only, knowledge-only, and source-less claims.
- Keyboard-only import through private draft creation works at all supported viewport baselines with
  visible focus, semantic status/error messages, 44px targets, and reduced motion.
- Private JSON archive schema v3 includes source and graph provenance in one read snapshot and excludes
  connection paths/future secrets.

Agent ownership is exclusive. Agents report changes and tests to the orchestrator and never edit this plan.

## Phase 5: open-source distribution and signed updates

Phase 5 makes Levelog safe to publish as an MIT-licensed GitHub repository and prepares a two-architecture
macOS release channel. The distribution decision was revised on 2026-07-20: until Apple Developer Program
membership is available, new users install an explicitly ad-hoc-signed, non-notarized DMG and follow the
documented macOS Privacy & Security approval flow. Existing users open Settings, check the fixed GitHub
Releases channel, inspect the version and plain-text release notes, then explicitly install a Tauri-signed
update and restart.

Distribution has two independent trust boundaries:

- Apple Developer ID plus notarization would protect browser-downloaded DMG/app execution and publisher
  identity, but is intentionally deferred. Ad-hoc signing does not provide publisher identity and does not
  remove Gatekeeper approval.
- Tauri updater minisign protects the `.app.tar.gz` consumed by an existing Levelog installation and remains
  mandatory even while Apple signing is deferred.

The GitHub Release job stops when any Tauri update key input is missing; it never emits an unsigned updater
artifact. Apple credentials are not required for the current distribution mode, and the build explicitly uses
the ad-hoc identity rather than silently falling back. Local development builds remain usable without secrets
and expose the updater as intentionally unconfigured. A future Developer ID migration is a deliberate release
mode change with its own verification gate.

The updater endpoint and public key are injected into the Rust crate at release compile time as
`LEVELOG_UPDATER_ENDPOINT` and `LEVELOG_UPDATER_PUBLIC_KEY`. The React webview cannot provide or override an
endpoint, key, download URL, or binary. Custom Tauri Commands build the updater with a 30-second check timeout,
hold at most one verified pending release, stream download progress, install only after an explicit click,
and restart after signature verification and replacement.

The same public key and HTTPS endpoint generate a private-material-free Tauri bundler config under the ignored
`src-tauri/target` tree. Each architecture's `.app.tar.gz` is then verified against its `.sig` with the same
Minisign algorithm used by `tauri-plugin-updater`. The final Draft gate also binds both `latest.json` entries to
distinct archives and exact signature assets in the current repository/tag before publishing.

### Phase 5 task ledger

| ID | Owner | Status | Depends on | Area |
| --- | --- | --- | --- | --- |
| P5-AUDIT | Orchestrator | completed | P4-VERIFY | tracked tree/history/asset/privacy/secret/publication audit |
| P5-UPDATER | Orchestrator | completed | P5-AUDIT | Rust updater boundary, Settings UI, progress and tests |
| P5-OSS | Orchestrator | completed | P5-AUDIT | MIT, README, security/contribution policy, audit tooling |
| P5-CI | Orchestrator | completed | P5-UPDATER/P5-OSS | least-privilege CI, SHA-pinned Release, Dependabot |
| P5-ICON | Orchestrator | completed | P5-AUDIT | original Levelog icon, all Tauri variants, app/DMG bundle validation |
| P5-LEGAL | Orchestrator | completed | P5-OSS | locked dependency license validation and bundled notices |
| P5-VERIFY | Orchestrator | completed | P5-CI/P5-ICON/P5-LEGAL | local frontend/Rust/Playwright/arm64 DMG verification |
| P5-HISTORY | User + Orchestrator | completed | P5-AUDIT | verified local backup, no-reply history rewrite, empty GitHub origin |
| P5-SOURCE-PUBLISH | Orchestrator | completed | P5-HISTORY/P5-VERIFY | audited no-reply source commit pushed to origin/main without a Release tag |
| P5-UNSIGNED-DESIGN | Orchestrator | completed | P5-SOURCE-PUBLISH | explicit ad-hoc DMG plus mandatory Tauri updater trust boundary |
| P5-UNSIGNED-WORKFLOW | Agent unsigned_release_implementation | completed | P5-UNSIGNED-DESIGN | Release workflow, generated bundler config, explicit ad-hoc signing, cryptographic artifact gate |
| P5-UNSIGNED-UX | Agent updater_unsigned_implementation | completed | P5-UNSIGNED-DESIGN | accurate updater verification and distribution-status UI/tests |
| P5-UNSIGNED-DOCS | Agent unsigned_distribution_docs | completed | P5-UNSIGNED-DESIGN | install warning, safe first-open flow, security/runbook/audit updates |
| P5-UNSIGNED-REVIEW | Orchestrator + independent reviewer | completed | P5-UNSIGNED-WORKFLOW/P5-UNSIGNED-UX/P5-UNSIGNED-DOCS | integrated security review, P2/P3 remediation, closure re-review |
| P5-PUBLISH | User + Orchestrator | in_progress | P5-UNSIGNED-REVIEW | updater key/Secrets completed; commit, hosted CI, tag Release and installed-update smoke remain |

Phase 5 acceptance requires:

- `pnpm audit:public` finds no tracked secret/private path and no unapproved personal commit email.
- `package.json`, Cargo, and Tauri versions match the `v<version>` tag.
- CI runs with read-only repository contents; Release alone receives `contents: write`.
- Every GitHub Action is pinned to a reviewed full commit SHA and Dependabot monitors updates.
- Release builds both `aarch64-apple-darwin` and `x86_64-apple-darwin`, DMGs, updater archives/signatures,
  and a multi-platform `latest.json`.
- Missing Tauri updater key inputs stops Release rather than producing an unsigned updater artifact.
- Release configuration explicitly selects ad-hoc macOS signing and every public surface discloses that the
  DMG is not Developer ID signed or Apple-notarized.
- The app never accepts an updater URL, public key, or artifact path from the webview.
- The Settings journey covers unconfigured development builds, no-update, update-available, progress, error,
  and user-confirmed restart states with accessible status output and plain-text release notes.
- A previous release build is manually updated through a valid Tauri signature and retains its local database before a public
  Release is marked complete.

Operational details and unresolved publication blockers live in `docs/release-runbook.md` and
`docs/publication-audit.md`.

## Verification gates

Required before completion:

```text
pnpm lint
pnpm test
pnpm build
pnpm test:e2e
cargo fmt --check --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml
pnpm tauri build
```

Playwright viewport coverage: 1586x992, 1280x800, and 1024x720. Tests cover keyboard focus and reduced motion. Real Codex smoke testing is a separate explicit command and is never part of the default test suite:

```text
LEVELOG_CODEX_PATH=/absolute/path/to/codex pnpm test:codex-smoke
```

## Current progress and handoff

Completed:

- Requirements, design document, and mock image were inspected.
- Technology and environment prerequisites were checked: Node, pnpm, Rust, Cargo, Xcode, and Codex CLI are installed.
- Codex CLI supports the required non-interactive, schema, ephemeral, sandbox, config/rules isolation, and output flags in the installed version.
- Design-system guidance was generated for the dark data-dense React dashboard.
- Created the implementation handoff document as the first repository change.
- Initialized Git and scaffolded locked pnpm/Cargo dependencies for Tauri, React, Rust, and tests.
- Verified `pnpm lint`, the initial Vitest suite, `pnpm build`, and `cargo check`.
- Wave 1 added the responsive HUD shell and SVG charts, the SQLite/domain/application layer, and the fail-closed Codex process boundary.
- Integrated candidate edits, quest template IDs, reflection difficulty, and recoverable adjusted/rescheduled quest transitions.
- Verified the integrated Rust suite: 11 tests passed.
- Added managed Tauri state and purpose-specific Commands for onboarding, dashboard, activities, asynchronous/cancellable Codex analysis, confirmation, quests, and reflections.
- Wave 2 added boot-gated onboarding, activity capture, editable send preview, resumable analysis states, explicit candidate accept/edit/reject decisions, dashboard, all quest states and reflection, the fixed 15-skill observation view, settings, manual/daily backups, and JSON export.
- The orchestrator integrated all routes and commands, added persisted Codex path changes, enforced a single global Codex execution permit, bounded an entire job to 180 seconds, made queued/running work cancellable, and limited retries to one JSON/schema retry.
- Quest proposals now receive only user-confirmed evidence rather than rejected raw candidates. Every analysis candidate requires exactly one explicit decision before confirmation.
- Quest transition plus schedule/time changes are atomic. Reflection is accepted only after completion, but `completed`, `partially_completed`, `not_completed`, and `rested` all receive the same idempotent `+40 XP` event.
- Dashboard day boundaries use local-day to UTC ranges; the central growth core shows today's XP and confirmed evidence count. Responsive screenshots verify the three specified layout bands.
- Added a fail-closed, ignored real-Codex smoke test, callable only through the explicit `pnpm test:codex-smoke` command.
- Phase 3 Wave 1 added bounded executable discovery with canonical-path validation, append-only rich
  profile revisions, editable focus themes, and separate Codex connection settings. The onboarding and
  Settings screens now share the same profile editor, so personal context remains editable after first run.
- Phase 3 Wave 1 added immutable one-line captures, persisted workflow/inbox state, analysis structure
  revisions, append-only interview answers, at-most-one structured question, and canonical plus specialized
  skill evidence. Confirmation remains blocked until the latest analysis reaches `review_pending`.
- Registered every Wave 1 command and made startup recovery fail pending/running jobs back to an assessable
  workflow. The activity-analysis v2 prompt/schema now separates confirmed and unconfirmed facts.
- Phase 3 Wave 2 connected the one-line capture, persisted organization inbox, structured one-question
  interview, explicit candidate decisions, canonical/specialized skill editing, and exact quest preview.
  A question can be answered, marked unknown, skipped, or deferred without losing the original capture.
- Quest generation now records every exact submitted payload and parsed proposal in `quest_generation_runs`;
  interrupted/failed runs remain auditable. JSON export schema v2 includes all Phase 3 history plus migration
  metadata while excluding Codex connection paths and future secret settings.
- Added three-viewport deterministic journeys for editable profile/Codex discovery, quick capture/inbox,
  all interview answer states, candidate decisions, quest preview, keyboard focus, reduced motion, and
  horizontal overflow.
- Independent review findings were resolved: terminal analysis refreshes workflow state; interview answers
  invalidate the exact preview; cancellation cannot publish late results; bundled schemas are embedded and
  copied into the execution sandbox; strict v2 output fields are locally validated; successful and invalid
  raw AI output are retained; empty edited evidence is rejected; and exact original capture whitespace is
  preserved.
- Onboarding now saves the profile before optional Codex setup, and Settings exposes profile conflict recovery.
  Manual-path mode cannot leave an invisible path selected. The installed CLI was found at both Codex.app and
  Volta locations; its current `exec --help` and feature list contain every required fail-closed control.
- Quest creation and generation-run completion are one transaction. JSON export is one read snapshot.
  Database, backup, and export directories/files use private macOS permissions, and completed backup/export
  files are published atomically. Failed/schema-invalid AI output remains available in the audit/export trail.
- Web Interface Guidelines review added a skip link, touch/hover/active states, balanced long headings,
  named form controls, a two-step destructive quest action, and persistent 44px capture actions. The updated
  responsive reference images were inspected before being accepted.
- Phase 4 replaced the Memory placeholder with a local Evidence Library. Pasted and native-picker-selected
  UTF-8 Markdown/text are preserved as immutable, SHA-256-addressed originals; identical content reuses the
  document while retaining every source occurrence. File intake rejects arbitrary webview paths, symlinks,
  unsupported/non-UTF-8/NUL content, and per-file/batch/count limits.
- Users can create excerpt-backed claims without AI, or explicitly preview/edit a Codex payload after a
  deterministic secret warning. Codex extraction keeps direct statements and inference distinct, inserts only
  pending candidates, stores payload/raw/version audit data, and has queue-inclusive 180-second timeout,
  cancellation, late-result protection, atomic persistence, and one schema-only retry.
- Claim review now supports accept, immutable edit-successor, reject, reversible assessment exclusion/defer,
  and reopen. Imported knowledge, ideas, inferred tendencies, and inference can be retained without becoming
  skill performance, XP, project truth, or portfolio material.
- Added explicit, reversible project/claim and claim/claim relationships. No semantic name match silently
  merges projects or claims. The fixed 5x15 skills remain optional observation links rather than the graph's
  ontology, and no Phase 4 operation writes XP or `skill_observations`.
- Private portfolio drafts are composed only from accepted, source-backed, user-approved eligible claims and
  remain editable/local. Evidence, Projects, Source provenance, review queue, and Portfolio are reachable from
  the app IA. JSON private archive schema v3 includes every Phase 4 table in one read snapshot.
- Two independent review passes plus targeted rework closed all P0/P1 findings: action/state contract mismatch,
  direct accepted project links, reversible exclusion/defer, unsafe portfolio kinds, structured secret warnings,
  source-switch state leakage, evidence relationship CRUD, relation endpoint resolution, and route discoverability.
- Phase 5 added an MIT open-source surface, public README, contribution/security policies, Changelog,
  Dependabot, secret/path/history audit tooling, and release metadata checks. The current source/asset tree has
  no detected token, key, credential, private absolute path, runtime fixture, or external trademark; the audit
  intentionally remains red only for the existing personal commit author email.
- Added a Rust-owned signed updater boundary. The endpoint and public key are compiled into Release builds,
  cannot be supplied by the webview, require HTTPS, keep at most one checked update, stream bounded download
  progress, verify Tauri signatures, and restart only after an explicit Settings action. Development builds
  clearly report that the channel is not configured.
- Added SHA-pinned CI and a two-architecture macOS Release pipeline. Missing Tauri updater credentials fails
  closed; Apple credentials are intentionally unnecessary. Artifacts remain a Draft until both DMGs, both
  cryptographically verified updater archives/signatures, tag-bound `latest.json` entries, and DMG checksums
  exist.
- Created a repository-owned Levelog icon from the Personal Evidence Graph concept: connected confirmed,
  active, and future evidence nodes over an upward layered form. The transparent 1254px source, provenance,
  and generated platform variants are tracked; the app bundle now declares and contains a 1024px ICNS.
- Added deterministic third-party notice generation for the locked npm/Cargo graph. The build fails on an
  unknown or selected strong-copyleft/source-available expression, deduplicates repeated notice text, and
  bundles both Levelog's MIT License and the generated notices into the app.

Phase 3 Wave 1 verification performed on 2026-07-20:

```text
cargo fmt                                      passed
cargo check --all-targets                      passed
cargo test                                     passed: 25 tests; real smoke ignored
pnpm lint                                      passed
pnpm test                                      passed: 9 tests
pnpm build                                     pending P3-D activity review type/UI integration
```

Phase 3 integrated verification performed on 2026-07-20:

```text
pnpm lint                                      passed
pnpm test                                      passed: 9 tests
pnpm build                                     passed
Playwright non-snapshot journeys               passed: 24 tests / 3 viewports
cargo fmt                                      passed
cargo clippy --all-targets -- -D warnings      passed
cargo test                                     passed: 28 tests; real smoke ignored
legacy duplicate-job migration                 passed; old job failed, newest preserved
```

Historical baseline final verification performed on 2026-07-20:

```text
pnpm lint                                      passed
pnpm test                                      passed: 7 tests
pnpm build                                     passed
pnpm test:e2e                                  passed: 15 tests / 3 viewports
cargo fmt --check                              passed
cargo clippy --all-targets -- -D warnings      passed
cargo test                                     passed: 16 tests; real smoke ignored
pnpm tauri build                               passed
Levelog.app binary                             Mach-O 64-bit executable arm64
Levelog.app launch                             passed; process observed running
```

Phase 3 final verification performed on 2026-07-20:

```text
pnpm lint                                      passed
pnpm test                                      passed: 11 tests
pnpm build                                     passed
pnpm test:e2e                                  passed: 30 tests / 3 viewports
cargo fmt --check                              passed
cargo clippy --all-targets -- -D warnings      passed
cargo test                                     passed: 32 tests; 1 real smoke ignored
pnpm tauri build                               passed
Levelog.app binary                             Mach-O 64-bit executable arm64
Levelog.app launch                             passed; PID 99994 observed running
Web Interface Guidelines audit                 passed after fixes
```

Phase 4 final verification performed on 2026-07-20:

```text
pnpm lint                                      passed
pnpm test                                      passed: 16 tests
pnpm build                                     passed
pnpm test:e2e                                  passed: 33 tests / 3 viewports
cargo fmt --check                              passed
cargo clippy --all-targets -- -D warnings      passed
cargo test                                     passed: 36 tests; 1 real smoke ignored
pnpm tauri build                               passed
Levelog.app binary                             Mach-O 64-bit executable arm64
Levelog.app launch                             passed; new PID 8373 observed, then smoke instance closed
Independent P4 closure review                  passed: no remaining P0/P1
```

Phase 5 local verification performed on 2026-07-20:

```text
pnpm check:release                            passed
pnpm notices:generate                         passed: 848 entries / 237 unique notice texts
pnpm lint                                     passed
pnpm test                                     passed: 20 tests
pnpm build                                    passed
pnpm test:e2e                                 passed: 33 tests / 3 viewports
cargo fmt --check                             passed
cargo clippy --locked --all-targets           passed with -D warnings
cargo test --locked                           passed: 40 tests; real Codex and release artifact smokes ignored
release config generator tests                passed: 5 tests
ephemeral-key updater verifier                passed: 1 cryptographic artifact verification
pnpm tauri build                              passed: Levelog.app + arm64 DMG
ad-hoc Release config smoke                   passed: arm64 app/DMG/app.tar.gz/.sig without Apple credentials
Levelog.app bundle                            arm64, com.levelog.desktop, v0.1.0
Levelog.app resources                         icon.icns 1024px + exact LICENSE/notices copies
hdiutil verify                                passed: DMG checksum valid
pnpm audit:public                             passed: 174 tracked files / 2 approved author emails
independent unsigned-release review           passed: no remaining P0/P1/P2
first hosted dual-architecture attempt        failed closed after both builds: verifier received relative paths
release verifier path regression              fixed: GITHUB_WORKSPACE absolute paths + static/runtime guards
ad-hoc dual-architecture release              pending corrected hosted tag workflow
live update from previous release build       pending first two Tauri-signed Releases
```

Playwright baselines are stored under `tests/e2e/shell.spec.ts-snapshots/` for 1586x992, 1280x800, and 1024x720.

Known issues and deliberate limits:

- The local `.app` is linker ad-hoc signed and not notarized. Public Releases intentionally use explicit
  ad-hoc signing until Apple Developer Program membership is available. Users must approve the first launch
  in macOS Privacy & Security; this is a disclosed limitation, not publisher verification.
- The public `main` history now uses one GitHub no-reply identity and `origin` targets the confirmed-empty
  `nozomi2255/levelog` repository. The replaced unpublished history exists only in a verified local `.git`
  bundle and must never be added to the repository.
- The long-lived Tauri updater keypair has been generated by the user, and the three required repository
  Actions Secret names were confirmed through GitHub without reading their values. Encrypted backup and
  password custody remain the user's operational responsibility. The `release` Environment now requires
  explicit approval from repository owner `nozomi2255` before either architecture build can access them.
- The audited source is published at `https://github.com/nozomi2255/levelog`. The first `v0.1.0` candidate tag
  points to the failed verifier attempt and its Release is still Draft; it will be replaced with the corrected
  commit before any public artifact exists.
- The first hosted CI exposed a pnpm configuration mismatch because the lockfile recorded
  `injectWorkspacePackages: true` from the development environment while the clean runner default differed.
  Repository-level `.npmrc` now fixes that lockfile input explicitly so local, CI, Release, and Dependabot
  frozen installs use the same setting.
- Hosted Linux Playwright correctly ran 30 functional journeys but could not consume target-specific Darwin
  image baselines. The functional job now skips only the three screenshot assertions outside macOS, while a
  required parallel `macOS visual regression` job runs those three viewport comparisons on the distribution
  platform. Visual coverage is separated by platform rather than disabled.
- GitHub-hosted macOS produced stable 2,281/3,629/3,943-pixel rasterization differences from the locally
  approved images across retries. The comparison allows at most 5,000 changed pixels per full-page viewport,
  while semantic/layout/overflow assertions remain exact; an official full-SHA-pinned artifact step retains
  actual, diff, and trace output for seven days whenever that bounded gate fails.
- Intel packaging and a real previous-version-to-current-version update cannot be reproduced locally on the
  current Apple Silicon host without the updater release credentials. The Release workflow builds both and
  keeps them Draft until its completeness gate passes; manual post-release validation remains required.
- The first hosted `v0.1.0` attempt built and uploaded both architectures but failed closed before publication:
  Cargo changed the verifier test working directory, so repository-relative signature paths did not resolve.
  The Release remained Draft. The workflow now passes `GITHUB_WORKSPACE`-absolute paths, the Rust verifier
  rejects relative inputs, and `pnpm check:release` guards this CI boundary before a tag is published.
- A live Codex inference was not sent during the default verification. It requires an authenticated compatible CLI and the explicit opt-in smoke command above.
- Import currently supports paste and individually selected `.md`, `.markdown`, and `.txt` UTF-8 files. Lossless
  format adapters for Obsidian folders/ZIP, PDF, Word, CSV/JSON semantics, and background sync are not enabled.
- The secret scanner is a deterministic preflight warning, not a guarantee that all confidential text is found.
  Users still review the exact editable payload before any optional Codex request.
- `evidence_analysis_jobs.model` remains nullable because Codex CLI does not expose a reliable resolved default
  model name. Levelog does not fabricate one; provider, CLI version, prompt/schema versions, payload, and raw
  schema output remain auditable.
- Reports, restore/import, notifications, semantic search, graph visualization, publishing/sharing, and
  cross-experience calibrated skill profiles remain future increments. Reports stays an explicit empty route.
- In the managed Codex sandbox, Playwright requires permission to bind the local Vite port; the suite itself passes after that permission is granted.

Next task:

> Commit and push the absolute-path verifier fix, confirm hosted CI, then replace the never-published `v0.1.0`
> tag and failed Draft with the corrected commit. Inspect both architecture assets, `SHA256SUMS.txt`, updater
> signatures, and `latest.json` before closing the first public Release; a previous-version in-app update
> remains pending until a second version exists.

## Resume procedure

When resuming after an interrupted session:

1. Read this file completely.
2. Run `git status --short --branch` and `rg --files`.
3. Find the first `in_progress` task in the ledger. If none exists, read `Known issues and deliberate limits` and `Next task` before starting new scope.
4. Run the most local test for that task, then continue it; do not restart completed work.
5. Before dispatching an agent, confirm its ownership does not overlap another live agent.
6. After integration, update the ledger, completed work, verification results, known issues, and `Next task`.
